class ScoreItem {
  bool used = false;
  int score;
  bool possibility = false;
  bool firstScore = true;

  int index;
  int col;

  ScoreItem(this.used, this.score, this.possibility, this.col, this.index, this.firstScore);
}
